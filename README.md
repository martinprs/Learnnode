# Learnnode
Environment variable for Node.js to disable warnings <br>
`NODE_OPTIONS=--disable-warning=ExperimentalWarning --disable-warning=DeprecationWarning` <br>
<br>
Install packages
```bash
npm i
```
Run live server
```bash
npm run start
```
